spring:
  application:
    name: helpdesk
  datasource:
    url: jdbc:mysql://localhost:3306/helpdesk
    username: root
    password: ''
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: none
    properties:
      hibernate:
        format-sql: true
